this is how you're creating a query set. In the product list view, the logic for creating this query set is different. Now, what is the second difference? The second difference is the serializer we're using for serializing data. So here we're using the collection serializer. In the product list view, we're using the product serializer. Right? Everything else is exactly the same. Now, we have the same situation when creating a resource.