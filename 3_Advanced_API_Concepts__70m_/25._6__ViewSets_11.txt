module called read only model view set. So we can import this. Now, if we inherit from this class, we can only perform read operations. We can list all collections or retrieve a single one, but we're not going to be able to create, update, or delete a collection. Okay. So just be aware of that. For now, we don't need it. So I'm going to use the model view set class here. Alright, we're done with this lesson. Next, we're going to talk about the model