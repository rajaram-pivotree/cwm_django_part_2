the naming convention. So we have the name of our resource followed by view set. Now this class should inherit model view set. Now, because this is also a generic view, here we have query set and serializer class attributes. So I'm going to move these two lines up here. Now, once again, because this is a generic API view, we also have this method get serializer context. So let's move