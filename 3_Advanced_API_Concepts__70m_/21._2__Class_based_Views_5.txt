And then we're going to move these few lines to our new method. Okay, we're done with this view function. We don't need it anymore. So the first benefit of a class-based view is that here we don't have those if statements. You know, as our application gets more complex, we end up with many nested if statements and they are really ugly. So the first benefit of class-based,