the hood that gets called, but when writing our code, we use a class and that means we get access to all object oriented programming features. Now, before we move on to the next lesson, I want you to convert this view function to a class-based view. That's super easy, so spend a couple of minutes on this, then come back to my solution. Alright, here's what I've done. I've created a new class called Product Detail with three methods, Get, Put, and