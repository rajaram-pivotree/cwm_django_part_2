Let's import the list model mixin as well as the create model mixin. We're going to look at the implementations of these classes. So if you're using a Mac, hold down the command key, if you're on Windows, hold down the control key, and then click on list model mixin. So this is a class with a single method called list. Now in this method, we have the logic for listing a bunch of models.