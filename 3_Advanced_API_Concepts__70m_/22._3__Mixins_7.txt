on Mac or Shift, Control and On Windows to see all the symbols in this module. So we're going to look at Create Model Mixing. Now, in this class, we have a Create method that encapsulates the logic for creating a resource. So first, we're creating a serializer, then we're validating the incoming data. Next we have a call to perform Create method, which is another method in this class and