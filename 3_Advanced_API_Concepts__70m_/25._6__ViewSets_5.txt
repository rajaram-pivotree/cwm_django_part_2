generic API view, which you are familiar with. So everything you have learned about generic views also exists in generic view sets. So here we have those attributes like query set and serializer class and everything else you have learned so far. So back to our view, let's see how we can combine the logic for these two views using a view set. We're gonna create a new class called product,