title and products count. We don't want to specify a products count when creating a collection. So if I just pass the title here and remove this property, look, we get an error when posting this to the server. We get a bad request saying products count is a required field. How can we solve this problem? Well, here in the collection serializer class, when defining this field, you want to