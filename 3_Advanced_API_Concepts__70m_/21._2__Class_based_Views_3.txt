utilizing the first letter of every word in contrast for function based views. We're using a different convention. We're using lower case letters and we're separating multiple words using an underscore. Okay. Now in this class, we're going to define two methods a get method for handling a get request and a post method for handling a post request. So the incoming request gets automatically dispatched to one of these methods depending on the