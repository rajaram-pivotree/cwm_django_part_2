From rest framework, nested, we're going to import the router's module. Now in this module, we have a bunch of router classes. So we have default router that replaces the default router that be imported from the rest framework. We also have nested default router. Now similarly, we have simple router and nested simple router. So we're going to use the default router that's in the router.