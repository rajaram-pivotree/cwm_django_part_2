is to suppress the warning, which I person don't like. So we have to do some research and find a specific error code and include that in the rest framework settings. So we have to suppress that error code or that warning code. I don't like that approach. The other approach is to create a custom pagination class and set the page size there. Let me show you how we can do this. So here in the store app, we're gonna add a new file called pagination.