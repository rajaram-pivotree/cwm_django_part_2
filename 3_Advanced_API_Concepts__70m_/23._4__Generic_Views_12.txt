easier to use the serializer class attribute. We set it to product serializer, okay? So we can delete these two methods. We're only gonna keep this method because we don't have an attribute for specifying the serializer context. It's only possible by overwriting this method, okay? Now back to the browser, let's refresh. So we have the same functionality as before. We can see all of our products, but let me show you something really cool.