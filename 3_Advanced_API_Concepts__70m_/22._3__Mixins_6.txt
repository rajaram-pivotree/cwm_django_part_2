Like here we have a call to filter query set. Don't worry about this. We'll talk about filtering and pagination later. If you leave out the details, you can see that here we're creating a query set. Then we're creating a serializer with that query set. And finally we're returning the serialized data. This is very similar to the pattern we had in our code. Now let's look at the implementation of the other mixing. So you can hold down shift,