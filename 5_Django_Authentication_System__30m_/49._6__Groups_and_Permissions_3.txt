this permission table. Let's take a look here. So down the bottom, look at the last record, can view user. So because we created a custom user model, Django created this permission cross. Now this permission has a unique identifier, a name, a descriptive name. It also has a code name. This is also a unique identifier, but it's a string that we can reference in the code. Now here, you can see the content type ID.