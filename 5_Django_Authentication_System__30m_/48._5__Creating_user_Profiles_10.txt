default value or we can create an added default value in our model. We don't want a hard code at default value in our model because otherwise every time we create a new customer, that customer will be associated with a specific user. We don't want that. So here I'm going to select the first option and supply a one off default value. I'm going to use one, which is the idea of the admin user. So let's go ahead with that.