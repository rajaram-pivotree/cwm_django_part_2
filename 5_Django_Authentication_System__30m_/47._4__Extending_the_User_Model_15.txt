to, semicolon, and here we add another statement, create database, storefront to. Now we select both of them and execute in one go. Okay, now back to the terminal, let's migrate our database one more time. Okay, this stem all migrations were applied successfully. Now back to the first terminal window, our web server is running beautiful, but there are a couple of issues we need to fix in the