Okay, we have a new migration. Now let's run it. So Python manage the pie migrate. Now we get a different error saying migration admin initial is applied before its dependency core initial and database default. So the reason this is happening is because we decided to swap out the user model in the middle of the project. So this migration, the first migration in the admin module is dependent on the