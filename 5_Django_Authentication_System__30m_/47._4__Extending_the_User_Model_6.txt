to import the abstract user class. In the previous lesson, I made a mistake. I said, we should extend the user class, but technically we should extend the abstract user class. Okay? So we extend this class. And here we redefine the email field. So we say email is models.email field. And here we apply a unique constraint. Beautiful. Now we need to tell Django that we're going to use this