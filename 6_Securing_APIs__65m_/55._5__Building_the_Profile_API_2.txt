have a profile called customer. All it's concerned about is the authentication of users, nothing more. So we need to build the customer API or the profile API ourselves, and that's super easy. So let's do that real quick. Back to VS Code. Now here's a question, where should we implement that API? In the store app, because this is where we have defined the concept of a customer, so the API belongs to this app as well. So