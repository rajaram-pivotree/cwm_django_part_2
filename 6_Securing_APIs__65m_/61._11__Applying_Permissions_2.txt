is authenticated or read only, and we can also create our own custom permission classes. I'm going to talk about that in the next lesson. But for now, let's see how we can use these permission classes. One way is to apply them globally. So here in the settings module, in the settings for the rest framework, just like we have default authentication classes, we can also add default permission classes. So we set this to a top wall or a list.