the local storage and send them in future requests. We'll talk about that later in this section. But what about logging out? Well, to log out the user, all we have to do is remove the tokens from the client, nothing else. So there's no endpoints we're gonna call to log the user out, because these tokens are not stored in a database. That's why we're using JSON web tokens. In the next lesson, I'm gonna demystify that for you.