are structured completely eliminates a database call because every token has a digital signature and on the server we can use that signature to ensure this is a valid token. So back to the documentation, let's go to the authentication back ends page. Here we have instructions for token-based authentication that comes with rest framework as well as instructions for JSON web token authentication.