HS256. What it is, it doesn't really matter, I don't even know. Now the second part is the payload. Once again here we have a JSON object with four properties. Token type is access because this is an access token, but if this was a refresh token, we would see refresh here. Then we have the expiration date time, and this value represents one day from now. Next we have JTI, which is a unique identifier for this token.