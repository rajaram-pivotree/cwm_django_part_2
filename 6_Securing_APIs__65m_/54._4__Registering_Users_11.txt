the space class and also set the user attribute here, but this is a better practice to inherit everything in the base class and overwrite specific parts because in the next version, the implementation of the middle class might change. So we want to inherit all that functionality and change some bits, okay? So now we have a serializer next we need to register it in the settings module. So let's go to the settings module. Down the bottom, let's