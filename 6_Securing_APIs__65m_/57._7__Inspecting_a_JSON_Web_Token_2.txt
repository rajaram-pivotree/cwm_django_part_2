simple JWT which is for Django. But if you're building your backend using a different platform like Node or ASP.NET, then there are other libraries you can use to generate and validate JSON Web Tokens. So what is this debugger? Well, look here on the left side, we can paste a JSON Web Token and see it decoded on the right side. So I'm gonna paste my JSON Web Token. On the right side,