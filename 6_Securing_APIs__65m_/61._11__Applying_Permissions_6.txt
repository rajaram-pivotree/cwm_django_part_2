So now all actions in this view set are closed for anonymous users. But once again, if we want to, we can override this permission in this particular action. So here we can set another argument called permission classes. Again, we set it to a list of permission classes. We don't want to do this, so let's remove that. Now, what if we want to have different permissions for different actions like create,