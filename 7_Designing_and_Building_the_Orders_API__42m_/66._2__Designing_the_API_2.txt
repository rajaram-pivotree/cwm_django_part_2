point should only be open to authenticated users. So using cart ID and user ID, we can create an order object and then return it to the client. Now we should also support get requests at this end point, but what the user sees depends on their permission. If I'm a regular user, I should only be able to see my own orders, but if I'm an admin, I should be able to see all orders in the database. Now we should also be able to retrieve a specific