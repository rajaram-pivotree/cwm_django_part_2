Alright, let's spend a couple minutes and plan out what we're going to build in this section. So we're going to build a new endpoint for managing orders. Now to create an order, we simply send a post request to this endpoint. Now all we need to include in the request body is the cart ID. Because we can extract the current users ID from the JSON web token included in the request header.