Now the final step, we want to delete a cart item. As you can see, this functionality is already implemented for us because our cart item view set extends the model view set class. And as you have learned, this class provides all operations. And also over here, we're allowing delete requests.