One of these are the internal representation of a resource on the server. In other words, on the server, we identify a resource like a product using an object or an instance of a Python class. But when we return this object to the client, we're going to represent it as HTML, XML, or JSON. Because these are the formats that clients understand, okay? Now, rest doesn't dictate what format or representation we should use. It's internally up to us.