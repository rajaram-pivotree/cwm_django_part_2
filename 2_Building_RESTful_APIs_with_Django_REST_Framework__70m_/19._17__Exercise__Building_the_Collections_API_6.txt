function that we created earlier in the course, I just had to implement it. So here we're going to get the collection with the given ID. Now as the first argument that is function, I'm passing a query set, the same query set that I used earlier. So we get all our collections and annotate them with the number of products in each collection. Now one more thing that you need to pay attention to here