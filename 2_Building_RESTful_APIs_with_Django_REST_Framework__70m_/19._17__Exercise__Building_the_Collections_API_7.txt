and goes convention for naming related fields. So in our models module, this is where we have defined the product class. Like here we have the collection field. As product defining this field, I set the related name to products. So now each collection has an attribute called product. Okay, now, like to review function. So we get a collection, then if you have a get request, you still realize the collection and return the data to the client.