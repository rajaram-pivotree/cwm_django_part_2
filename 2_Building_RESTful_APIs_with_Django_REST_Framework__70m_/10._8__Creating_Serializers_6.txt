serializer fields, you can see all types of fields that are available to us. So we have Boolean fields, String fields, like Charfield, Email field and so on. We also have New Mac fields, Daytime fields and so on. So pretty much everything that we have for creating a model is also available here. Now all these fields have a bunch of core arguments that are available everywhere, like read only, write only,