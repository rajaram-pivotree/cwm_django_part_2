the idea of the collection. Beautiful. That's one way to serialize a relationship. Another way is to return a collection as a string, so we can return the name of each collection here. So back to our serializer, instead of a primary key related field, we're going to use a string related field. So Django will convert each collection to a string object and return it here. Now what is the string representation of a collection?