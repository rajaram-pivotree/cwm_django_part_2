product object and store it in the database. Let's see how this works. So back to our product list, your function. First, we need to pass an argument to this API view decorator. We have to pass an array of strings that specify the HTTP methods with support at this endpoint. So get and post for creating data. Now previously we didn't have to do this because get is supported by default.