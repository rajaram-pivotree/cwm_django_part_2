We're going to insert it as a JSON object in the body of the request. So the server will rate this product and create it. Now let's say we want to update product number one. First we need to ask are we going to update all properties or just a subset of them? If you want to update all properties, we should send a put request. Otherwise, we should send a patch request to this URL. Note that this URL is referencing a specific product and not the product's