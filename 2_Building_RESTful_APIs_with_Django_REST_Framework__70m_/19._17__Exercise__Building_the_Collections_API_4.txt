first part of this course. So if you don't remember this concept, go back and watch that lesson in the first part. So we annotate our collections and get a query set. Then we send that to our collection serializer for serialization and return the serializer data in the response. Again, exactly like how we implemented the product's endpoint. Now as part of this, I had to modify the collection serializer. So here in the list of fields I added,