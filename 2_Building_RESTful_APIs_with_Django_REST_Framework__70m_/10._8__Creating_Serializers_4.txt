class. Look, our product class has all these fields, but what we return from our API doesn't necessarily need to have all these fields. Because what we have here is the internal representation of a product, but what we return from our API is the external representation of a product. Sometimes you might have sensitive information here, we don't want to expose them to the outside world. That is why we need to separate representations of a product, an internal