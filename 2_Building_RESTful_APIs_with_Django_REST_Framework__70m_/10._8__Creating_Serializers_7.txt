default, we can also assign default values and so on, but certain fields, depending on their type, also support additional arguments. Again, exactly like how we define a model. So back to the code, let's define another field called title. We're going to set these two serializers that chart field, and here I'm going to set max length to 255. Now you might be wondering why we need to specify that