on the top, from rest framework, the decorators, we're gonna import the API view decorator. Now, if we apply this decorator to our view function like this, the request object that we receive here will be an instance of the request class that comes with the rest framework. So this will replace the request object in Django with the newer request object that comes with the rest framework, which is simpler and more powerful.