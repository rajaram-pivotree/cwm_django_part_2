So this is how we can create a view function in Django. We create a function that takes a request and returns an instance of the HTTP response class in Django. So in Django, we have two classes HTTP request and HTTP response. But Django's framework also comes with its own request and response classes. These classes are simpler and more powerful than the ones that come in Django. Let me show you how to use them.