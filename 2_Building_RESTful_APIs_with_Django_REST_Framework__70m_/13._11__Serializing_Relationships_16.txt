saying hyperlink related field requires the request in serializer context. So we need to pass our request object to our serializer because the request contains information about URLs. So the exception is giving us some hint. It's saying add context equals this object when instantiating the serializer. So back to our views module. This is the view function for retrieving all products.