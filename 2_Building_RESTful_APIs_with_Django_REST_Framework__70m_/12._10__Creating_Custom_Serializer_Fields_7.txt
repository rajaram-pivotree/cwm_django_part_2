Auto save. Okay, that makes our job easier. Now refresh. All right, we got an exception. Unsupported operand types for multiplication. Basically, the reason this is happening is because we're multiplying a decimal value by float value. Because the unit price of a product is defined as a decimal, but this number in Python is by default a float. So we need to convert it to a decimal