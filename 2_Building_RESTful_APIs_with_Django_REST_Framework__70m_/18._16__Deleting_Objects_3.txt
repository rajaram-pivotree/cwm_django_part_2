And then we're going to return an empty response with the status of HTTP 204, which means no content. Again, this is one of the conventions of RESTful APIs. So quite often, when we delete a resource, we return an empty response with the status of 204. Now, this is not written in stone. So in your application, you might prefer to return the object that was deleted. That is totally fine. Now let's test it.