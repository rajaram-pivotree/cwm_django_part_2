the title of each collection for each product, Django is sending an extra query to retrieve the collection of that product. So to solve this problem, we need to load products and their collections together. Now, where should we do this? Well, let's go back to our views. So this is the view for getting the list of products. And over here, we have a query set for retrieving products.